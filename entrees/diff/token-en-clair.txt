commit 0000000000000000000000000000000000000000
Author: entrée synthétique <entrees@salsi.local>
Date:   Thu Aug 6 09:12:44 2026 +0200

    ci: appeler l'API de livraison depuis le job de deploy

    ENTRÉE SYNTHÉTIQUE — la seule de la banque. On ne récolte pas une vraie
    fuite de secret : le jeton ci-dessous est le même faux jeton que celui de
    fixtures/invalid/L007-secret-en-dur.yaml, et il n'a jamais existé.

diff --git a/.gitlab-ci.yml b/.gitlab-ci.yml
index 3f1a2b9..8c4d0e1 100644
--- a/.gitlab-ci.yml
+++ b/.gitlab-ci.yml
@@ -18,6 +18,13 @@ build:
     - mvn -B package -DskipTests
   artifacts:
     paths: [target/*.jar]
+
+deploy:
+  stage: deploy
+  script:
+    - curl -sS -X POST https://api.interne.example/v1/livraison
+        -H "Authorization: Bearer ghp_AbCdEfGhIjKlMnOpQrStUvWxYz01"
+        -d "ref=$CI_COMMIT_SHA"
+  only: [main]
diff --git a/src/main/resources/application.yml b/src/main/resources/application.yml
index 7d2c1a4..b91e5f3 100644
--- a/src/main/resources/application.yml
+++ b/src/main/resources/application.yml
@@ -4,3 +4,5 @@ spring:
     url: jdbc:postgresql://localhost:5432/demo
     username: demo
+livraison:
+  token: ghp_AbCdEfGhIjKlMnOpQrStUvWxYz01
